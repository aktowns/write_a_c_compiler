module Stage8.Valid.Continue;

fn main(): Int32 {
    let sum: Int32 = 0;
    for (let i: Int32 = 0; i < 10; i = i + 1) {
        if (sum % 2)
            continue;
        sum = sum + i;
    }
    return sum;
}
