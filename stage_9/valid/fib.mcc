module Stage9.Valid.Fib;

fn fib(n: Int32): Int32 {
    if (n == 0 || n == 1) {
        return n;
    } else {
        return fib(n - 1) + fib(n - 2);
    }
}

fn main(): Int32 {
    let n: Int32 = 5;
    return fib(n);
}
